apiVersion: v1
kind: Service
metadata:
  name: nodejs-oidc-playground
  labels:
    app: nodejs-oidc-playground
    app.kubernetes.io/name: nodejs-oidc-playground
    app.kubernetes.io/component: nodejs-frontend
    app.kubernetes.io/part-of: nodejs-playground
spec:
  selector:
    app: nodejs-oidc-playground
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
